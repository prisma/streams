diff --git a/src/shard/record.rs b/src/shard/record.rs
--- a/src/shard/record.rs
+++ b/src/shard/record.rs
@@ -57,7 +57,7 @@
     if key.len() != prefix.len().saturating_add(8) {
         return Err(RecordCorruption::KeyWidth);
     }
-    if !key.starts_with(prefix) {
+    if !key.starts_with(&prefix[..16]) {
         return Err(RecordCorruption::Namespace);
     }
     let offset = u64::from_be_bytes(
