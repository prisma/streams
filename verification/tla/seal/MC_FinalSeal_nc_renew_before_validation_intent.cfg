\* TLA-003 NEGATIVE CONTROL (TLA-003-F4 pre-fix) on shape V4:
\* OwedRetryValidatedFirst <- NothingValidatedFirst (the owed exact retry renews the claim in
\* prepare_close, before parse_content refuses it for B's ingest capacity).
\* Expect IntentOnlyAfterValidation violated.
CONSTANTS
    X = X  Y = Y  Z = Z  S = S  W = W  N0 = N0  N1 = N1  N2 = N2  L = L  LX = LX  LY = LY  A = A  B = B  CX = CX  CY = CY  CN = CN
    NONE = NONE  PLAIN = PLAIN
    FinalOps <- V4FinalOps
    PlainOps <- VPlainOps
    AppendOps <- VAppendOps
    Surface <- VSurface
    Content <- VContent
    Producer <- VProducer
    SeqOf <- VSeqOf
    Slots <- VSlots
    Budget <- VBudget
    Unbounded <- MCNoUnbounded
    Validity <- V4Validity
    Procs <- TwoProcs
    HomeOf <- HomeSplitX
    InitOwner = A
    MaxGen = 5
    MaxCrash = 1
    MaxRetire = 0
    MaxCancel = 1
    MaxTimeout = 0
    MaxRegFault = 0
    MaxEnqFail = 0
    MaxFenceReadFail = 0
    MaxHeldFence = 0
    MaxClaimLoop = 0
    WitnessMode = FALSE
CONSTANT OwedRetryValidatedFirst <- NothingValidatedFirst
SPECIFICATION Spec
CONSTRAINT GenBound
INVARIANTS
    IntentOnlyAfterValidation
