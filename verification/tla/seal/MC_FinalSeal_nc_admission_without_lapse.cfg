\* TLA-003 NEGATIVE CONTROL on shape PW: ClaimedFinalPlan <- AdmissionPlanKept.
\* Restricted to behaviours in which no lease lapses (CONSTRAINT LeaseNeverLapses).
\* Expect FinalClosedTruthful violated.
CONSTANTS
    X = X  Y = Y  Z = Z  S = S  W = W  N0 = N0  N1 = N1  N2 = N2  L = L  LX = LX  LY = LY  A = A  B = B  CX = CX  CY = CY  CN = CN
    NONE = NONE  PLAIN = PLAIN
    FinalOps <- PWFinalOps
    PlainOps <- VPlainOps
    AppendOps <- PWAppendOps
    Surface <- PWSurface
    Content <- PWContent
    Producer <- PWProducer
    SeqOf <- PWSeqOf
    Slots <- PWSlots
    Budget <- PWBudget
    Unbounded <- MCNoUnbounded
    Validity <- AllOk
    Procs <- OneProc
    HomeOf <- HomeA
    InitOwner = A
    MaxGen = 5
    MaxCrash = 1
    MaxRetire = 0
    MaxCancel = 1
    MaxTimeout = 1
    MaxRegFault = 0
    MaxEnqFail = 0
    MaxFenceReadFail = 0
    MaxHeldFence = 0
    MaxClaimLoop = 0
    WitnessMode = FALSE
CONSTANT ClaimedFinalPlan <- AdmissionPlanKept
SPECIFICATION Spec
CONSTRAINT GenBound
CONSTRAINT LeaseNeverLapses
INVARIANTS
    FinalClosedTruthful
