\* TLA-006 negative control "nc_admit_after_close" (small shape): a registration slot exists after the close boundary (guard-restating form).
\* ONE target so the verdict is attributable: RetiredEngineFrozen
SPECIFICATION Spec
CONSTANTS
    Reqs = {"r1", "r2", "r3"}
    Writers = {"r1", "r3"}
    MaxWalFail = 1
    MaxWriteFail = 1
    MaxRetire = 1
    MaxAbort = 1
    PublicationOp <- MutPublicationAlways
    StrandedOp <- RealStranded
    AttachOp <- RealAttach
    TakeOp <- RealTake
    WriteErrorRetires <- RealWriteErrorRetires
INVARIANT
    RetiredEngineFrozen
