\* TLA-019 (timing-lapse shape: compactor checkpoint of 1 tick) ASSUMPTION PROBE (ASM-SLATEDB-GC (iii), not production): the upstream read of a deleted SST returns a short success. Expect NoFalseCompleteRead violated: the repository cannot detect it.
CONSTANTS
    MinAge = 1
    Horizon = 2
    MaxTime = 4
    CkLife = 1
    PollInterval = 1
    ReadSpan = 1
    UseCheckpoint = FALSE
    AllowOrphan = TRUE
    AllowReader = TRUE
CONSTANT UpstreamDeletedRead <- MutUpstreamShortRead
SPECIFICATION Spec
INVARIANT
    NoFalseCompleteRead
