\* TLA-018 KNOWN DEFECT TLA-018-F3 (open): the applied unfiltered expanded shape. A continuation cursor minted over an applied suffix that the ownership move lost is accepted once the new owner's tail has passed it. Expect ExactDurablePrefix violated until the cursor format carries the minting owner or durable frontier.
CONSTANTS
    N = 3
    InitNext = 1
    KeyOf <- MCKeyOf
    Size <- MCSize
    Req = 2
    Filter = "none"
    Mode = "applied"
    MaxPend = 2
    MaxMoves = 1
    MaxPages = 5
    MaxLoops = 2
    TrimBudgets = {0, 1, 3}
    AllowRing = TRUE
    AllowCorrupt = FALSE
    AllowShortIndex = FALSE
    AllowLostPostings = FALSE
    AllowLostCanonical = FALSE
    AllowReadError = FALSE
SPECIFICATION Spec
INVARIANT
    ExactDurablePrefix
