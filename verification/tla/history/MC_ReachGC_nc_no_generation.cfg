\* TLA-019 UPSTREAM-CONTRACT negative control: delete from an old reachability snapshot by min_age only (no low-watermark / newest-L0 generation condition). Expect ManifestRefsPresent violated.
CONSTANTS
    MinAge = 1
    Horizon = 2
    MaxTime = 4
    CkLife = 3
    PollInterval = 1
    ReadSpan = 1
    UseCheckpoint = FALSE
    AllowOrphan = TRUE
    AllowReader = TRUE
CONSTANT GcEligible <- MutEligibleAgeOnly
SPECIFICATION Spec
INVARIANT
    ManifestRefsPresent
