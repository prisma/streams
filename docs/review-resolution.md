# Review remediation ledger

This work addresses the 24 findings in the supplied `prisma-streams-code-review.md`, against original commit `a7e2070f3b4346b3e54d552069ff91c56e900130` on `slate`. Commits are scoped to one finding; findings with several owners or follow-up corrections have multiple commits. The original review is input evidence, not authorization to deploy infrastructure or certify an independent audit.

## Changes by finding

| Finding | Implemented change | Primary commits | Evidence |
| --- | --- | --- | --- |
| R01 | New segment-scoped AES-256-GCM-SIV frames with stored random nonces; legacy frame decoding retained. | `031ae5b` | Crypto vectors, repeated-nonce/split/rollback and tamper tests; [storage evidence](review-storage-evidence.md). |
| R02 | Typed append commands, outcomes and failures shared by both API adapters; dedicated validation, lifecycle, admission, routing and submission phases; removed response decoding and invented offsets. | `bfa038b`, `ff8d222` | Direct owner tests and existing producer, admission, lifecycle and protocol scenarios; [application evidence](review-application-evidence.md). |
| R03 | Distinct append, close and fence commands; pure producer/queue/billing decisions and an explicit durable-effects plan; queue refusals obey the group durability boundary. | `fd1706e`, `59f0508` | Failed-group and actual remote-WAL barriers; [storage evidence](review-storage-evidence.md). |
| R04 | Persisted descriptor DTO converted into immutable validated serving state; validated topology and claim generations; unknown segments cannot acquire default routes. | `120b6d3`, `bfa61e9` | Descriptor/topology/DTO golden tests and unchanged seal assertions; [application evidence](review-application-evidence.md). |
| R05 | Explicit creation, fork, deletion, topology and seal coordinators; fenced claims and cancellation debt; raw/product final-record completion shares lifecycle policy. | `e9b488d`, `1850b0a`, `fe2100c`, `fc19b61`, `8c8f717`, `886d45a` | [Creation transitions](creation-transitions.md), [seal transitions](seal-transitions.md), crash/cancellation/incarnation tests. |
| R06 | Canonical query plans and typed pages with mandatory consumed/durable progress; shared replay, long-poll, fork and lineage execution; typed peer adapter. | `2f7e9ec`, `77f1012` | Cold history, filtered progress, applied rollback, fork/SSE/remote and public read scenarios; [application evidence](review-application-evidence.md). |
| R07 | Consumer and watch application owners; explicit authorization states, consumer incarnation contexts, DLQ retry debt, verified watch observation and exactly-once subscription admission. | `c5f8ca0`, `f2f1d69`, `c0c39f7` | [Consumer evidence](review-consumer-evidence.md), [watch evidence](review-watch-evidence.md). |
| R08 | One typed conditional-write/CAS primitive, required conditional tokens, typed storage/fencing failures, and checked persisted queue/billing codecs. | `6f3782b`, `e8b4257`, `59e1484`, `8b44407` | CAS conflicts versus ordinary errors, missing ETags, malformed queue state; [storage evidence](review-storage-evidence.md). |
| R09 | Bounded and fair controller passes and journal bytes; cancellable active storage work; joined shard absorbers; durable retry debt across cancellation. | `01ef5d0`, `e17ee57`, `e3b8f82`, `dab94e7`, `a5ffbe5`, `bdf4951` | Controller boundary/fairness/cancellation tests; [runtime evidence](review-runtime-evidence.md). |
| R10 | Runtime-owned history/postings/telemetry caches, admission and store gates, HTTP body ceilings, journals/alerts, readiness progress, and scaler clocks/state. | `b612fbf`, `79186e3`, `96fed7c` | Early-access/two-runtime capacities, release/restart, actual database/cache lifetime, body/admission and journal isolation tests; [runtime resources](runtime-resources.md), [usage ownership](review-usage-ownership-evidence.md). |
| R11 | Relocated the DST monolith into contract owners, explicit fixtures and pure models; recursive multitenancy audit and preserved test/scenario inventories. | `eba6a0f` | [Test evidence](review-tests-evidence.md), exact test-body and scenario inventories. |
| R12 | Malformed persisted tails fail closed; fixed-width cursor decoding returns errors instead of panicking. | `78c5940` | Corrupt-tail and malformed-width storage tests; [storage evidence](review-storage-evidence.md). |
| R13 | Required billing reads/decode failures reject the accounting group instead of creating empty state. | `d985f00` | Injected read/corruption failures with atomic state assertions; [storage evidence](review-storage-evidence.md). |
| R14 | Rollup and financial endpoint reads propagate failures before ledger/cursor advancement. | `814d3a5` | Read/decode failure, unchanged-progress and recovery tests; [storage evidence](review-storage-evidence.md). |
| R15 | Authenticate/admit mutation bodies before collection; never poll bodyless or refused watch/OPTIONS bodies. | `2d23065`, `012ba73` | Real HTTP entry with oversized, never-finishing body sentinels, including valid and expired signed capabilities. |
| R16 | Uniform unauthorized watch responses across descriptor existence and malformed carrier/query combinations. | `025fadb` | 144-request status/header/body equivalence matrix; [application evidence](review-application-evidence.md). |
| R17 | Critical task termination, shutdown and missing monitors fail readiness at both serving endpoints. | `7f8bcf9`, `ba71066` | Task-monitor state tests and real `/health`/`/readyz` HTTP 503 tests. |
| R18 | Bounded incarnation cooldowns and sketch state; deterministic aggregate hot-key selection. | `cc270e7` | Capacity/expiry/incarnation and insertion-order regressions; [runtime evidence](review-runtime-evidence.md). |
| R19 | Concurrent expired requests share refresh generations; failed/stale refresh completions cannot overwrite valid credentials. | `6a79dfb` | Controlled expiry, late 401, provider failure and stale completion tests; [SDK evidence](review-sdk-evidence.md). |
| R20 | Producer sends, retries, state import/reset and epoch bumps serialize through one bounded queue. | `29daf44` | Deferred completion/epoch race and sparse-key eviction controls; [SDK evidence](review-sdk-evidence.md). |
| R21 | Consumer iterator cleanup settles recorded decisions, aborts pending pulls and preserves original plus cleanup failures. | `6573da9` | Early break, thrown body, pending-pull cancellation and failed settlement tests; [SDK evidence](review-sdk-evidence.md). |
| R22 | Wrong-cell responses are classified before generic retry, including subscriptions; only transport failures follow retry policy. | `544deee` | Header-only 503 and subscription routing tests; [SDK evidence](review-sdk-evidence.md). |
| R23 | Month closing seeks directly after the persisted exclusive cursor instead of rescanning completed prefixes. | `320478d` | Bounded chunks and linear visit-count regression; [storage evidence](review-storage-evidence.md). |
| R24 | Fixed-baseline architecture ratchet, mechanism-aware scenarios, exact-set oracle negative controls and clean-HEAD execution receipts. | `5cc0e86` | [Test evidence](review-tests-evidence.md), architecture/scenario/inventory/evidence verifier mutation tests. |

## Verification status

The source freeze passed all-target Clippy with no new warning fingerprints, formatting, the architecture ratchet and the multitenancy audit. Per-owner documents record focused executed checks and their mechanisms. Final combined release, capacity, SDK and conformance execution receipts live outside the source checkout so they cannot change the tree they certify. The task handoff reports their results and locations; these focused checks are not substitutes for that combined run.

Independent cryptographic review, retained production-ciphertext auditing, and live Prisma Compute/Tigris workload/field campaigns are not certified by this local remediation. The scenario map retains partial, external and unmapped obligations explicitly. No local test result is presented as those external acceptance legs.
