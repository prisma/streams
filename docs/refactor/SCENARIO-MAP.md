# Scenario Map Summary (WP-00 deliverable 6)

**GENERATED by `scripts/scenario-map-report.py` from
`test-scenario-map.json` — do not hand-edit counts.** The JSON is
authoritative; this file is a view. Validation (same IDs as
`docs/dst/SCENARIO-CATALOG.md`, unique, mapped-iff-tests,
coverage values, test-symbol existence) runs with `--check`.

Catalogue source: `docs/dst/SCENARIO-CATALOG.md` (189 scenarios).

## Counts

- scenarios inventoried: **189**
- mapped to concrete tests: **138** (full 111, partial 25, external 2)
- unmapped (no concrete test): **51**

Status labels as recorded in the catalogue:
- Existing: 87
- L1-now: 30
- Existing/strengthen: 19
- L2-sim: 14
- Strengthen: 4
- Existing/L2: 3
- L2-sim/performance: 3
- L2 performance: 3
- L2-sim/L3-field: 2
- Existing oracle control: 1
- L1-now/L2-sim: 1
- Existing but strengthen handshake: 1
- Existing/property: 1
- Existing family: 1
- Existing mechanism gate: 1
- Existing field/focused: 1
- Existing campaign/property: 1
- Existing fork/campaign gate: 1
- L1-now/fork: 1
- Existing/static + L3: 1
- Existing/outer-loop: 1
- Existing CI: 1
- Existing benchmark test: 1
- L1/SDK: 1
- Existing single-node: 1
- Existing campaign/test: 1
- Existing local/liveness: 1
- L2 CPU model/L3 benchmark: 1
- Existing performance gate: 1
- Existing campaign/fork: 1
- Existing campaign: 1
- Future implementation gate: 1
- L2/L3: 1

## Unmapped scenarios

These have no concrete test in the tree. L2-sim/L3-field
entries await the simulator/fleet harnesses by design;
Existing/Strengthen/L1-now entries are the gaps that matter
(see notes in the JSON).

- **COST-001** (Existing performance gate): WAL request amortization — Campaign-level: bench/costab (run-soak.sh/run-wide.sh). In-suite supporting pin only: object_classes_match_production_telemetry:198 (classifier matches production telemetry).
- **COST-003** (Existing campaign): Shared history cardinality slope — Campaign-level: bench/costab (run-mature.sh/run-wide.sh).
- **COST-006** (Future implementation gate): Sparse L0 timer/recovery budget — Future gate; src/main.rs:1610 unflushed_at_or_below_l0_is_rejected_before_any_engine_opens is a config guard, not the budget.
- **COST-007** (L2/L3): Physical attempt ledger
- **COST-008** (L2 performance): Read fanout does not multiply origin GETs linearly — Nearest in-suite fanout economics: 32317 (two subscribers, one source read) and livefeed_seal_retry_is_one_task_per_feed_at_fanout:34585; the 1/100/10k Class B bound is L2.
- **CRT-008** (L1-now): Create-and-close crash matrix — GAP: no create-with-close crash-boundary matrix found (no test combines create and close lifecycle phases).
- **CRT-009** (L2-sim): Initialization takeover is cancellation-proof
- **DUR-014** (L2-sim): Acknowledged-set equality, not count equality — The exact-set oracle exists (oracle_catches_loss:2404, oracle_catches_duplicates:2416) but the count-preserving loss+duplicate swap leg is not a test; needs the simulator.
- **FLT-004** (L2-sim): Ring convergence under staggered cold starts — The 371,900-record loss shape is referenced; single-node wedge repro (2619) is the nearest analogue, not the fleet scenario.
- **FLT-005** (L2-sim): Stale router replay bound
- **FLT-006** (L2-sim): Ownership handoff with unabsorbed data — Single-node analogue exists (untouched_streams_absorb_after_restart:3870); fleet leg needs the simulator.
- **FLT-009** (L2-sim): Node crash versus pause
- **FLT-010** (L2-sim): Fleet desired-state CAS conflict
- **FLT-011** (L2-sim/L3-field): Scale out/in with shard movement
- **FLT-012** (L2-sim/L3-field): Half-fleet loss under peak load
- **FLT-013** (L2 performance): Stable fleet request-cost bound
- **FLT-014** (L2-sim): Router and server disagree on map version during split — Nearest single-node leg: seal_gap_stale_descriptor_redispatches:6407 (stale map refresh + redispatch).
- **FLT-015** (L2-sim): Heal phase convergence
- **FRK-004** (Existing): Cycle and depth defense — GAP (Existing with no test): MAX_FORK_DEPTH=64 exists in src/http.rs:2727 but no test builds a cycle or over-depth chain.
- **FRK-008** (L1-now): Child init crash after tail seed — GAP: Fp::InitBeforeSeed park exists but is only used by the CRT-007 test (create seed); no fork-child crash-after-tail-seed resume test found.
- **FRK-012** (L1-now): Delete wins before fork reference — GAP: opposite ordering (delete decides first, fork CAS sees dead source) not implemented; fork_creation_and_source_deletion_serialize has only the one ordering.
- **FRK-017** (L1-now): Ready fork idempotent PUT after source soft-delete — GAP: idempotent fork PUT leg exists (22687) but BEFORE soft-delete; the 'exact target PUT returns existing fork after source soft-delete' leg is not tested.
- **FRK-019** (L1-now): Expiry with fork references — GAP: no test for TTL-expiry of a source with live fork references.
- **FRK-020** (L2-sim): Fork object reachability under GC
- **HIS-016** (Existing campaign/property): Postings storage ratio — Campaign-level: bench/costab keyed drivers (run-keyed.sh, keyed-compare.py). In-suite mechanism pins: keyed_frames_no_longer_count_twice_against_the_budget:3800.
- **HIS-022** (Existing): Reader cache cold stampede — MECHANISM REPLACED: no history-reader-cache module exists in current src/ (superseded by LiveFeed + postings cache). Nearest equivalents: livefeed_two_subscribers_share_one_feed_and_one_source_read:32317, src/sse/registry.rs:221 last_subscriber_evicts_and_reconnect_does_not_grow, src/postings_cache.rs:929 warm_install_serves_from_zero_without_index_load.
- **HIS-023** (Existing): Reader cache stale stampede — MECHANISM REPLACED (see HIS-022). Nearest: src/postings_cache.rs:948 warm_extension_stitches_chunks / 1096 eviction_poisons_fresh_claims.
- **HIS-024** (Existing): All callers cancel, open still lands — MECHANISM REPLACED (see HIS-022). Nearest engine-open equivalents: open_gate_survives_impatient_clients_without_a_storm:2683 (clients give up, open completes), a_hung_open_is_deadlined_and_its_late_engine_reaped:3055.
- **HIS-025** (Existing): Probe error does not evict healthy reader — MECHANISM REPLACED (see HIS-022). Nearest: src/sse/feed.rs:1789 source_failure_is_typed_and_recoverable.
- **HIS-026** (Existing): Independent node/store caches — MECHANISM REPLACED (see HIS-022). No per-store reader-cache isolation test remains.
- **HIS-028** (Existing fork/campaign gate): GC cached inventory cannot suppress work — Campaign/fork-level per the label; no src test found for stale boot inventory + obsolete object rediscovery before TTL dead zone.
- **HIS-029** (L1-now/fork): Latest-version probe gap fallback — GAP: no test for the N known / N+1 missing / N+2 present probe-gap fallback.
- **HIS-030** (L2-sim/performance): GC concurrent delete throughput and partial failure
- **QUE-006** (L1-now): Retry and extend race — GAP: the settle API carries acks/retries/extends (src/product.rs:6763) but no test forces stale retry/ack/extend orderings; only settle-side stale fencing exists (9409, 24621).
- **QUE-008** (L1-now): DLQ target recreation — GAP: no test for delete/recreate of the DLQ target with pinned epoch. Adjacent: dead_letter_link_requires_a_shared_key:10561 (config-time gate only).
- **QUE-010** (L1-now): Consumer merge lineage — GAP: only split lineage (9604) exists; no consumer-across-merge test.
- **QUE-012** (L2-sim/performance): Bounded queue-state loading
- **RES-007** (L2 performance): Reject storm protection
- **RES-008** (L2 CPU model/L3 benchmark): Event-loop starvation model
- **SDK-003** (L1/SDK): Producer operation chain bounded — GAP: no eviction/boundedness test for the SDK producer operation chain (sdk/src/index.ts has no test harness; package.json has no test script).
- **SEC-002** (Existing/static + L3): Auth before body buffering — GAP in test code: no test sends an unauthenticated oversize (32 MiB) request and asserts rejection before body read. Nearest: inflight_admission_answers_only_after_authentication:36349 (auth precedes admission/tarpit, round-13) and the 401 legs of 22329.
- **SEC-008** (L1-now): Catalog transient GET failure — GAP: registry failpoints fail_next_get/fail_next_list exist but are used by the consumer-saga (24317) and quota-seed (29601) tests, not by a catalog page-walk test.
- **TOP-005** (L2-sim): Crash after phase-B publication before parent retirement
- **TOP-008** (Existing): Dominant key refuses split — GAP (Existing with no test): scaler3.rs implements hot-key suppression (scaler3.rs:238 '§5.2') and hot_key() surfacing (scaler3.rs:124), and dst_tests.rs:14335 deliberately avoids tripping it — but no test asserts a dominant key refuses split and surfaces hot_key. Nearest mechanism unit test: src/sketch.rs:290 spacesaving_finds_the_dominant_key (detection only).
- **TOP-013** (Existing/strengthen): Phase-B CAS uses pending-read epoch — GAP (Existing/strengthen with no direct test): no test recreates the name between pending-read and phase B. Adjacent: stale_scaler_and_ttl_decisions_decline_after_recreate:13703 (decision epoch fencing), but not the phase-B CAS epoch leg.
- **TOP-014** (L2-sim): Stale router replay
- **TOP-015** (L2-sim): Ring preference versus possession
- **TOP-017** (Existing field/focused): Split under load with response loss — Campaign-level: bench/costab/run-split.sh + split-driver.py. In-suite nearest (no fault injection): split_children_land_on_distinct_engines:7064.
- **TOP-020** (L2-sim): Repeated split/merge does not leak parents/tasks
- **WAT-005** (L1-now): Watch split lineage — GAP: no watch-definition-across-split test (SSE/livefeed lineage is covered; watches are not).
- **WAT-006** (L1-now): Watch definition lifecycle ABA — GAP: no delete/recreate-during-parked-watch-update test.

