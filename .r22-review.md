# Round-22 billing review (judge input — verify EACH item against the repo)

MUST-FIX ITEMS:
1. Tenant identity: deployment-global ACCOUNT_ID/PROJECT_ID is only valid if the model is one-project-per-cell. DECIDE now and formalize: either per-request authenticated principals persisted at creation, or one-project-per-cell documented as the deployment contract with per-request principals named as the precondition for shared cells. Billing identity must never come from stream names, client headers, or ambient defaults in a shared cell.
2. Read spool: (a) partial persistence failure must requeue the REMAINDER of drained batches, not just the current one; (b) required-mode readiness must include spool opened + pending rows readable + system client configured (no memory-only fallback window); (c) corrupt spool rows must be quarantined + surfaced + alert, never silently skipped.
3. Multi-segment idle-month carry: close_month pass A must use page-local row caches (like apply_page) so multiple segments of one stream in one page merge into ONE put; test with 3 segments over idle months asserting stream=sum, name agg=sum, project agg=sum.
4. Final/live snapshots sharing usage_version: global SegmentState must tie-break (same version + greater storage_accounted_through_ms wins) so the following month's carry uses the new gauge/boundary; test: rollover final + live same version, next month idle, close uses new gauge exactly.
5. Read-batch month split must be exact integer allocation across ALL intersecting UTC boundaries with remainder-to-last; invariant sum(allocations)==original per dimension; no double-billing from round(0.5)+round(0.5); handles batches spanning >2 months.
6. Month-final scan errors must fail that engine's drain contribution (no live-snapshot emit, no ack) — financial scans fail CLOSED, never unwrap_or_default to "nothing exists".
7. Terminal closure must be a durable saga: debt persisted at tombstone time with logical close timestamp; closes use the persisted time; awaited submission (not silently-dropped try_send); catalog/tombstone-walk reconciler discovers clean-marker terminal streams; retained_by_forks persisted durably; registry errors do not default to emit/ack.
8. Artifacts + corrections invoice-grade: AlreadyExists must verify the existing object's content before marking Published; missed months caught up in order (persisted oldest-unfinalized marker); UsageCorrection carries all dimensions + correction_id/version/source event ids/reason/created_at; effective totals (base+corrections) materialized and exposed in the usage API; immutable correction artifacts; aggregates carry correction sums.
9. Drain bound must be hard: envelopes AND encoded bytes, including month-final rows in the budget; month-close chunks bounded by rows and bytes.
10. BILLING_MODE=required readiness gate: spool ready + system client configured as instance readiness prerequisites; rollup DB open required on the rollup instance; a billing-readiness surface reporting fleet-level conditions (ledger reachable, cursor progressing, no spool corruption, close debt, pending artifacts).

DOC ITEMS:
D1. Split normative design vs status: OBSERVABILITY-BILLING.md (target/semantics) + OBSERVABILITY-BILLING-STATUS.md (implementation matrix, open gates, campaigns) — or CURRENT/PARTIAL/TARGET labels throughout.
D2. Document the account dimension in the monthly artifact path (telemetry/usage-monthly/<account>/<project>/<stream-id>/<YYYY-MM>.json).
D3. Project usage endpoint: implement GET /v1/projects/{project}/usage or mark it target/future.
D4. RUNBOOK.md: remove BILLING_STREAM*/METRICS_* and __metrics__; document USAGE_STREAM_KEY, BILLING_MODE, ACCOUNT_ID/PROJECT_ID/CELL_ID, ROLLUP, TELEMETRY_DRAIN_SECS, OUTBOX_SWEEP_SECS, MONTH_CLOSE_GRACE_MS, METRICS_INTERVAL_SECS, ALERT_USAGE_OUTBOX_DIRTY; add runbooks for spool failure/depth, outbox lag, ledger availability, rollup cursor lag, closure debt, pending artifacts, corrections, month-close progress, invoice readiness.
D5. docs/STAGING.md: provision USAGE_STREAM_KEY/BILLING_MODE=required/identity/ROLLUP=1 instead of METRICS_KEY/BILLING_STREAM_KEY.
D6. OPERATIONS.md: __metrics__ reconciliation reference replaced or moved to history.
D7. README doc map includes docs/OBSERVABILITY-BILLING.md.
D8. REPORT.md + EXPERIMENT-PILOT.md marked historical (banner or move).
D9. Release report carries one exact preview provenance block (commit, tag, pin, schema version, SDK hash, suite results, billing mode, rollup owner).
D10. "All eleven addressed" reworded to "initial implementations landed; invoice-grade requires the listed gates".

JUDGE PROTOCOL: for each of items 1-10 and D1-D10, verify against the CURRENT repo state with file:line evidence; verdict CLOSED or NOT-CLOSED with the exact deficiency. Financial-path code must fail closed. Tests named in items must exist and assert what the item says.
